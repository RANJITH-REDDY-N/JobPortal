{
  "buildCommand": "cd client && npm install && npm run build && cd ../server && pip install -r requirements.txt",
  "startCommand": "gunicorn your_project_name.wsgi --bind 0.0.0.0:$PORT",
  "installCommand": "cd client && npm install && cd ../server && pip install -r requirements.txt",
  "builds": [
    {
      "src": "server/*",
      "use": "@railway/python"
    },
    {
      "src": "client/*",
      "use": "@railway/node"
    }
  ]
}